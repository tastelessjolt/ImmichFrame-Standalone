<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  android:installLocation="auto"
  package="io.github.tastelessjolt.immichframestandalone"
  android:versionCode="10"
  android:versionName="1.0.15.0-standalone.10">

  <!-- Permissions -->
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.WAKE_LOCK" />
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
  <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
  <!-- Features -->
  <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
  <uses-feature android:name="android.software.leanback" android:required="false" />

  <application
    android:label="ImmichFrame Standalone"
    android:icon="@mipmap/immich_frame"
    android:banner="@mipmap/immich_banner"
    android:usesCleartextTraffic="true">

    <activity android:name="io.github.tastelessjolt.immichframestandalone.MainActivity"
              android:label="ImmichFrame Standalone"
              android:launchMode="singleTop"
              android:theme="@style/MyTheme.NoActionBar"
              android:exported="true"
              android:windowSoftInputMode="stateHidden|adjustResize">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
      </intent-filter>
    </activity>

    <service android:name="io.github.tastelessjolt.immichframestandalone.ScreenSaverService"
             android:exported="true"
             android:icon="@mipmap/immich_frame"
             android:label="ImmichFrame Standalone"
             android:permission="android.permission.BIND_DREAM_SERVICE">
      <intent-filter>
        <action android:name="android.service.dreams.DreamService" />
      </intent-filter>
    </service>

    <receiver android:name="io.github.tastelessjolt.immichframestandalone.ScreenScheduleReceiver"
              android:enabled="true"
              android:exported="false"
              android:process=":screen_schedule">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <action android:name="android.intent.action.TIME_SET" />
        <action android:name="android.intent.action.TIMEZONE_CHANGED" />
        <action android:name="android.intent.action.DATE_CHANGED" />
        <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
      </intent-filter>
    </receiver>

    <activity android:name="io.github.tastelessjolt.immichframestandalone.ScreenScheduleBootstrapActivity"
              android:enabled="true"
              android:exported="false"
              android:excludeFromRecents="true"
              android:noHistory="true"
              android:process=":screen_schedule"
              android:theme="@android:style/Theme.NoDisplay" />

  </application>

  <uses-sdk android:minSdkVersion="21" android:targetSdkVersion="34" />
</manifest>
